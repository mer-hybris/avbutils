# Build utilities for Android Verified Boot

**genvbmeta** - Generates vbmeta image for a set of images.
**avbpubkey2c** - Ouputs AVB public key as a C style array for embedding in code.

See --help on each tool for more usage information.


